[package]
name = "bicep"
version = "1.3.0"
edition = "2021"

[lib]
path = "src/bicep.rs"
crate-type = [ "cdylib" ]

[dependencies]
zed_extension_api = "0.6.0"

[lints.clippy]
all = { level = "warn", priority = -1 }
